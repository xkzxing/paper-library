# 实现说明

本文面向希望了解项目设计、评估隐私边界或参与维护的读者。它描述功能如何协作、数据保存在哪里以及失败时如何恢复；阅读本文不需要先逐个打开源码文件。

## 一、总体设计

应用采用“本地资料库是文件事实来源，应用数据库是可恢复索引”的设计。

- 用户选择一个普通文件夹作为资料库；PDF、普通笔记和精读笔记都保留在该文件夹中，用户可以在访达中直接查看。
- 应用使用 SwiftData（苹果的数据持久化框架）保存文献条目、分类、标签、阅读项目、文件版本和人工智能任务状态。
- `.paperlib/manifest.json` 是资料库的可移植清单。应用每次重要保存后自动导出清单，并保存有限数量的历史版本。
- 文件导入、移动和恢复不把绝对路径写入数据库，而是使用相对于资料库根目录的路径；每次由统一的路径校验逻辑重新解析。

```text
用户选择资料库
      │
      ├── 资料库文件：PDF、笔记、.paperlib 清单与操作记录
      │
      └── 应用数据库：文献关系、界面状态、任务状态与检索索引
                         │
                         └── 清单导出／数据库损坏时恢复
```

主入口位于 `PaperLibrary/App/PaperLibraryApp.swift`。它创建应用数据库，并注入资料库访问、文件健康检查和检索协调器；主窗口展示资料库，设置窗口负责资料库选择、联网服务与检索配置。

## 二、启动与资料库访问

### 选择资料库

用户通过系统文件选择器选定资料库目录。应用为该目录创建安全作用域书签，并把书签和资料库标识保存在用户偏好中。下次启动时先恢复书签，再开始访问该目录。

每个资料库根目录都有 `.paperlib/library.json`，其中包含唯一标识、结构版本和创建时间。重新选择目录时，应用会对照此标识，避免把另一套资料库误连到当前数据库。

### 路径安全

所有相对路径都由 `LibraryPathSafety` 校验：拒绝绝对路径、家目录缩写、空路径、`.`、`..`、符号链接和指向资料库外部的解析结果。需要打开的 PDF 还会检查扩展名和常规文件属性。

这层校验的目的有两个：一是防止损坏的清单或数据库使应用访问资料库外的文件；二是确保文件移动、恢复和健康检查使用同一套边界规则。

### 启动恢复顺序

资料库界面启动后按以下顺序执行维护工作：

1. 读取清单；若数据库为空或需要恢复，则从清单重建可恢复的记录。
2. 扫描未完成的导入事务，删除无效暂存文件或补齐可确认的记录。
3. 检查已登记 PDF 是否仍存在；缺失文件会按 SHA-256 尝试在资料库内重新定位。
4. 把此前异常中断的人工智能分析和精读任务标记为可恢复状态。
5. 重新建立或增量更新全文检索索引。

如果 SwiftData 数据库因损坏、迁移失败或不兼容而无法打开，应用会先把数据库及其日志移动到应用支持目录中的隔离位置，再创建新数据库，并尝试依据资料库清单恢复。这比直接删除数据库更便于排查和找回数据。

## 三、核心数据模型

| 对象 | 作用 | 主要关系 |
| --- | --- | --- |
| 文献 | 保存题名、作者、年份、标识符、期刊信息、状态与联网核对结果 | 可有多个文件版本、标签、个人标记、阅读项目、分析与精读任务 |
| 文件版本 | 保存相对路径、哈希、页数、大小、导入时间与首选标记 | 归属一篇文献 |
| 分类与标签 | 分类用于主要归档位置；标签用于多维筛选 | 文献可有多个标签 |
| 阅读项目 | 保存阅读组织、优先级、排序与已读状态 | 一篇文献可属于多个项目 |
| 人工智能分析 | 保存结构化研究卡、来源文件版本、用量、费用和异常状态 | 归属一篇文献 |
| 精读任务 | 保存计划、完成进度、恢复信息、用量和输出状态 | 归属一篇文献 |

文献与 PDF 不是一对一关系：同一篇文章可以保存预印本、修订版、附录版等多个文件版本，并由用户选定首选版本。这个设计使合并、拆分、查重和重新处理不必复制整条文献记录。

## 四、导入、归档与查重

### 导入流程

导入协调器接收一个或多个文件后，会依次完成：

1. 验证输入为可读 PDF，并用 PDFKit 提取题名、作者、摘要、页数、年份和可识别的 DOI 等元数据。
2. 计算 SHA-256、文件大小和页数，形成待审核条目。
3. 使用哈希、DOI、NBER、SSRN、arXiv、RePEc 与文本指纹寻找重复候选。
4. 让用户决定创建新文献、作为新版本合并，或跳过候选。
5. 根据分类、作者、年份和题名生成目标相对路径；元数据不足时进入 `Uncategorized/Unknown Year` 并保留原文件名。
6. 以带事务标识的暂存与提交步骤复制文件、写入记录、保存数据库，最后删除暂存状态。

源文件始终不被移动或改名。导入完成后，资料库内的副本才成为由应用管理的版本。

### 重命名与移动

当用户修改文献元数据、分类或归档位置时，应用以批次执行移动操作。PDF 的普通笔记、精读笔记和对应资源目录被视为伴随文件，会随 PDF 一起移动。移动前记录源路径与目标路径，执行失败时尝试回滚；下次启动还能依据操作记录继续清理或恢复。

### 删除、合并与拆分

删除文献会先处理关联文件版本和关系记录。合并用于将重复的文献记录集中到一篇文献下；拆分则将选定文件版本移到新文献。两种操作都以文件版本为粒度，因此不会改变原始 PDF 内容。

## 五、资料库清单、备份与健康检查

### 清单

清单当前有独立的结构版本，包含资料库标识、文献、文件版本、分类、标签、项目、人工智能状态和必要关系。它以格式化的 JSON 保存于 `.paperlib/manifest.json`，并在 `.paperlib/backups/` 保留最近的历史副本。

导出采用临时文件加原子替换方式，避免写入中断留下半份 JSON。界面层监听数据库保存事件，并对连续修改进行延迟合并，减少频繁磁盘写入；应用转入后台时会立即导出。

### 健康检查

健康检查只遍历资料库内部。它先验证每个记录的相对路径；若 PDF 缺失，才扫描资料库寻找相同 SHA-256 的候选文件。找回后更新相对路径；找不到则保留问题状态供用户处理，不会猜测性删除记录。

## 六、搜索与智能检索

### 基础检索

基础检索组合题名、作者、年份、摘要、标识符、文件路径和全文片段，支持全部关键词、任意关键词、完整短语与多种状态筛选。界面先根据当前分类、标签或阅读项目确定允许范围，再返回文献层面的结果。

### 全文索引

全文提取器逐页读取 PDF 文本并切分为带页码范围的段落。索引存储将以下信息写入独立的 SQLite 索引：

- 文献和文件版本标识；
- 段落序号、起止页、原文与规范化文本；
- 文件 SHA-256 和路径，用于判断索引是否仍然有效；
- 向量及其模型版本信息。

文件发生导入、删除、替换或移动时，协调器只使相关文献的索引失效并增量更新，而不是重建全部资料库。

### 智能检索

智能检索分为三个阶段：

```text
查询
  │
  ├── 关键词与书目信息评分
  ├── 查询向量与段落向量的相似度召回
  └── 候选融合、按文献去重、远程重排
                              │
                              └── 返回带页码和片段的文献结果
```

向量生成与重排通过百炼服务完成。应用先将候选段落分批发送生成向量，在本地保存规范化向量；查询时只发送查询文本，使用余弦相似度选出有限数量的候选。之后把候选片段和查询提交给重排服务，最终合并为“每篇文献一条结果、附若干证据片段”的展示。

快速、平衡、深度三档改变候选数量、字符预算和重排范围，不改变资料库原文。为控制内存与费用，流程始终限制进入下一阶段的候选数，并支持取消、重试和进度提示。

## 七、元数据与引用导出

### 本地提取和联网核对

PDF 导入后先使用本地解析结果。用户主动核对时，Crossref 客户端优先按 DOI 查询；缺少 DOI 时，按题名、作者和年份检索并做相似度匹配。通过核对的字段由独立应用器写回文献，冲突会保留提示，不会无条件覆盖用户已确认的信息。

期刊评价通过 OpenAlex 查询，结果以 JSON 缓存在文献记录中，并记录上次查询时间。这样列表显示不需要每次联网。

### BibTeX 导出

本地导出器从文献记录生成引用键和条目。在线核验模式先尝试 DOI 内容协商；失败后以 Semantic Scholar 搜索为后备。远程记录会被解析成字段，与本地题名、作者、年份和期刊逐项比较；界面展示差异，用户确认后再导出或更新。

网络请求对超时、限流和服务器错误执行有限重试，并且可被任务取消。

## 八、人工智能研究卡与精读笔记

### 研究卡

研究卡由 Gemini 分析器生成。提交前会检查接口密钥、文件可读性、文件大小、用户设定的月度预算和长文档策略。默认情况下，超过页数阈值的文章只发送前若干页；也可以跳过自动分析。

分析器上传 PDF 或节选，要求模型返回结构化内容，包括研究问题、方法、数据、结论、局限性和证据页码。页码规范化器把模型返回的页码映射为实际 PDF 页码，并标记无法验证的引用。结果、用量、费用估算和错误状态会写入人工智能分析记录。

批量分析按受控并发执行，并通过共享请求闸门避免多个耗时上传同时失控。取消不会影响本地文献文件。

### 精读笔记

精读笔记面向长文档，流程为：

1. 上传 PDF，并等待服务端文件可用；
2. 创建或复用远程缓存内容，生成章节计划；
3. 按计划逐段请求内容，以流式响应追加到本地检查点；
4. 每完成一段就记录进度、令牌用量和费用；
5. 成功后在 PDF 旁导出 Markdown 笔记，失败或取消时保留不完整笔记与检查点。

检查点存放在 PDF 所在目录的隐藏任务目录中，并记录任务标识、完成部分和计划。应用重启后会把运行中的任务标为中断；只要检查点仍在，用户可以从下一段继续，而不必重做已完成内容。

## 九、笔记、项目和界面状态

普通笔记和精读笔记是 PDF 旁的 Markdown 文件。第一次创建时使用安全的默认内容；已有文件不会被覆盖。资源目录同样跟随 PDF 管理。

阅读项目只保存“文献属于项目、优先级、已读状态和排序”等组织关系，不复制或移动 PDF。分类、标签、个人标记、侧栏选择、排序方式和搜索偏好分别保存在应用数据库或用户偏好中。

## 十、网络与隐私边界

应用启用了沙盒、用户选择目录读写、下载目录读写和客户端网络权限。它没有自有账户、同步、广告或行为分析服务。

联网动作及其最小数据范围如下：

| 功能 | 目标服务 | 可能发送的数据 |
| --- | --- | --- |
| 元数据核对 | Crossref、OpenAlex、DOI、Semantic Scholar | DOI、题名、作者、年份、期刊等书目信息 |
| 研究卡与精读 | Gemini | PDF 或节选、提示内容、模型请求参数 |
| 智能检索 | 百炼 | 查询、候选文本片段、工作区标识和鉴权请求 |

接口密钥不写入仓库。当前实现将其保存在应用支持目录的私有文件中，目录权限为 `0700`、文件权限为 `0600`。使用者仍应自行评估第三方服务政策，并避免上传不应外传的文献。

完整的用户视角说明见[隐私说明](PRIVACY.md)。

## 十一、测试与维护入口

测试覆盖导入及回滚、查重、移动恢复、笔记、清单恢复、元数据、引用导出、人工智能结果、精读检查点、检索选择和资料库健康检查。单元测试以内存数据库和临时资料库运行，避免读取真实文献。

维护时可按职责进入以下目录：

| 目标 | 主要目录 |
| --- | --- |
| 界面与交互 | `PaperLibrary/Features/` |
| 文献数据对象与归档规则 | `PaperLibrary/Domain/` |
| 文件导入、移动、恢复与健康检查 | `PaperLibrary/Services/FileStore/` |
| 元数据与引用服务 | `PaperLibrary/Services/Metadata/` |
| 人工智能分析与精读 | `PaperLibrary/Services/AI/` |
| 全文与智能检索 | `PaperLibrary/Services/Search/` |
| 清单备份与恢复 | `PaperLibrary/Persistence/Backup/` |

提交涉及文件格式、联网请求、权限或人工智能数据传输的改动时，请同时更新本说明、隐私说明和相应测试。
